name: Execute data interlock

on:
  workflow_dispatch:
    inputs:
      logLevel:
        description: "Log level"
        required: false
        default: "warning"
        type: choice
        options:
          - info
          - warning
          - debug
      tags:
        description: "Test scenario tags"
        required: false
        type: boolean
      environment:
        description: "Environment to run tests against"
        type: environment
        required: false
  repository_dispatch:
    types: [run-test-script]

jobs:
  run-script:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: node-version
        run : nvm use 18

      - name: Install TypeScript
        run: yarn add typescript --dev

      - name: Install ts-node
        run: yarn global add ts-node@10.9.1

      - name: Run Test Script
        run: $(yarn global bin)/ts-node src/dataInterlock.ts
